{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Invoices": {
    "InvoiceCodeMaxRetries": 3
  },
  "Stripe": {
    "_comment": "SecretKey and WebhookSigningSecret MUST come from user-secrets / env vars (Stripe__SecretKey, Stripe__WebhookSigningSecret) — never commit test/live keys. PublishableKey is public.",
    "SecretKey": "",
    "PublishableKey": "",
    "WebhookSigningSecret": "",
    "Currency": "PHP"
  }
}
